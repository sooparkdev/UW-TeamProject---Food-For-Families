// import { GoogleMap, useJsApiLoader } from '@react-google-maps/api';

const Map = () => {

    // console.log( process.env.REACT_MAP_GOOGLE_MAPS_API_KEY); /* check */
    // const { isLoaded } = useJsApiLoader({ /* this returns whether the map is loaded or not */
    //     id: 'google-map-script',
    //     googleMapsApiKey: process.env.REACT_MAP_GOOGLE_MAPS_API_KEY,
    // })

    // const seattle_coords = {lat: 47.6062, lng: 122.3321}

    return ( 
      <div className="map"></div>
    );
}
 
export default Map;

